# Applied to every pip run in CI through PIP_CONSTRAINT, including installs
# driven by scripts outside this repository.
#
# odoo:19.0 is ubuntu noble based: typing_extensions 4.10 comes from apt and has
# no RECORD file, so pip cannot replace it. anyio >= 4.15.0 requires
# typing_extensions >= 4.16.0, which makes the install fail with
# "Cannot uninstall typing_extensions 4.10.0, RECORD file not found".
anyio<4.15
